# 貢献方法

セットアップとテスト手順は [開発ガイド](docs/development.md)、処理の分担は [アーキテクチャ](docs/architecture.md) に記載しています。

## 変更を始める前に

- 既存の未コミット変更を確認し、他の作業を上書きしないでください。
- カメラ取得・推論・表示の分離を維持します。カメラのメーカー名や特定の機器 ID を条件にした分岐は追加しません。
- 開始要求のキャンセル後や機器切り替え後に、古いストリーム・認識結果が復帰しないようにします。
- 実行時の外部通信・録画・音声取得を追加する変更は、動作とプライバシーの説明も見直してください。

## テストとレビュー

機能変更には、その動作や失敗条件を再現するテストを追加します。ドキュメントだけの変更では、リンク・手順・実装との整合性を確認します。

```powershell
npm.cmd test
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
npm.cmd run test:e2e
```

パッケージ構成・ランチャー・認識アセットを変更した場合は、exe を再ビルドし、`LAUNCHER_EXE` を指定したブラウザーテストも実行します。

コミット前に次を確認します。

```powershell
git diff --check
git diff --cached --name-only
git diff --cached
```

[プライバシーの指針](docs/privacy.md) に従い、実機の型番・個人パス・ログ・画像・認証情報を含めないでください。公開用のコミット作成者情報も確認してください。モデルやテスト画像を追加する場合は、出典・ライセンス・チェックサムを記録します。

PR には、修正した問題、変更後の動作、実施したテスト、残る制約を記載してください。人物や室内が映るスクリーンショットは不要です。

## 不具合報告

[トラブルシューティング](docs/troubleshooting.md) を確認したうえで、再現手順と OS・ブラウザーの一般的なバージョン情報を報告してください。個別の端末識別子や秘密情報は公開 Issue に記載しないでください。

本プロジェクト自体のライセンスは未設定です。貢献や再配布に関する権利の扱いを、このガイドが独自に変更することはありません。
